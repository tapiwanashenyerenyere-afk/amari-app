// Minimal setup for pure-logic tests. No native/Expo mocks are pulled in
// here on purpose — the modules under test in __tests__/ have no
// React/native/Expo/Supabase runtime dependency, so nothing needs mocking.
